{
  "name": "termstructor",
  "version": "0.2.0",
  "type": "module",
  "private": true,
  "description": "Browser game that teaches terminal tools by playing",
  "scripts": {
    "serve": "python3 -m http.server 8080",
    "test": "node --test 'tests/**/*.test.js'",
    "lint": "find js -name '*.js' -print0 | xargs -0 -n1 node --check"
  },
  "engines": {
    "node": ">=18"
  }
}
